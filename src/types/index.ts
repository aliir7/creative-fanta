export type HeroType = {
  id: number;
  image: string;
  title: string;
  subtitle: string;
  price: string;
  model: string;
  bgColor: string;
};

export type Product = {
  id: number;
  image: string;
  title: string;
  desc: string;
  delay: number;
};
